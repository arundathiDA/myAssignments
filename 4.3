		Assignment 4.3

1. States = rownames(US Arrests)
Get states names with ‘w’.
Get states names with ‘W’.
Solution:
> st<-rownames(USArrests)
> grep("W",st)
> st[grep("W",st)]
 


2. Prepare a Histogram of the number of characters in each US state.
Solution:


> hist(nchar(states),main = Histogram",
 xlab="number of characters in US State names")
> positions_a = gregexpr(pattern = "a", text = states, ignore.case = TRUE)

> num_a = sapply(positions_a, function(x) ifelse(x[1] > 0, length(x), 0)) num_a

> library(stringr)
> str_count(states, "a")
> str_count(tolower(states), "a")
> vowels = c("a", "e", "i", "o", "u")
> num_vowels = vector(mode = "integer", length = 5)
> num_vowels = vector(mode = "integer", length = 5)
> for (j in seq_along(vowels)) {num_aux = str_count(tolower(states), vowels[j]) num_vowels[j] = sum(num_aux)
 }
> names(num_vowels) = vowels
> num_vowels
> barplot(num_vowels, main = "Number of vowels in USA States names",
        border = NA, ylim = c(0, 80))
